# 🚀 AIPE03 專案：MLOps 全自動化系統啟動說明書 (Demo 專用)

這份說明書記載了從零開始啟動整套「端到端 (End-to-End) MLOps 閉環系統」的標準作業程序 (SOP)。
建議在 8/11 預報或平時完整測試時，嚴格依照以下順序開啟終端機執行。

---

## 💻 準備工作
請確保你開啟了 **5 個獨立的終端機視窗 (或分頁)**，並且每個視窗都已經切換到專案的根目錄：
```bash
cd /Users/albert/Documents/專案/AIPE03
```
> **💡 小提醒**：如果執行腳本時遇到 `Permission denied`，請先執行 `chmod +x *.sh` 開放權限。

---

## 🛠️ 啟動順序

### 第 1 步：喚醒雲端核心大腦 (後端服務)
這會啟動資料庫、Kafka 訊息通道、後端 API 等基礎設施，是整套系統的心臟。
- **在哪裡執行**：第 1 個終端機
- **執行指令**：
  ```bash
  ./start_cloud.sh
  ```

### 第 2 步：啟動資料部門 (標註網頁 & AI 巡邏員)
這會開啟 Label Studio 網頁，同時在背景啟動 AI 巡邏員幫你自動畫骨架，以及負責監聽「100 張重訓門檻」的 Webhook 接線生。
- **在哪裡執行**：第 2 個終端機
- **執行指令**：
  ```bash
  ./setup_label_studio.sh
  ```

### 第 3 步：啟動煉丹工廠 (ClearML 伺服器)
這會把 ClearML 的任務看板跟網頁架設起來，讓你可以監控 MLOps 的自動訓練進度。
- **在哪裡執行**：第 3 個終端機
- **執行指令**：
  ```bash
  ./setup_clearml.sh
  ```

### 第 4 步：打卡上班 (喚醒 Mac M 晶片準備接單)
**⚠️ 此步驟極度重要！** 這是叫你的 Mac 隨時待命。只要第二步的接線生說「集滿 100 張了！」，這個工人就會立刻吃掉新資料並啟動訓練。
- **在哪裡執行**：第 4 個終端機
- **執行指令**：
  ```bash
  source Fall/.venv/bin/activate
  clearml-agent daemon --queue default --docker ""
  ```

### 第 5 步：開啟病房攝影機 (邊緣端推論)
這是最後一步！攝影機一開，就會開始捕捉畫面。如果它不小心誤判了，照片就會立刻飛到第二步的 Label Studio 讓你審核，完美閉環正式開始運轉！
> **🤖 硬體智慧加速**：腳本內建了硬體自動偵測機制。當偵測到 NVIDIA GPU (N卡) 時，會自動啟用 **DeepStream 產線級引擎** 進行高速推論；若無 N 卡，則自動無縫切換為 **GStreamer + OpenCV (Python)** 模式，確保在任何環境下皆能順利運行！
- **在哪裡執行**：第 5 個終端機
- **執行指令**：
  ```bash
  ./start_edge.sh
  ```

---

## 🎯 Demo 展演必殺技 (建議)
當你在台上向評審展示系統時，強烈建議使用以下雙螢幕配置：

1. **左半邊螢幕**：放置 `Label Studio` 的標註網頁。
2. **右半邊螢幕**：放置 `ClearML` 的 Tasks 列表網頁。

**🎤 展演說詞範例：**
> 「各位評審，請看螢幕。我們的系統實現了完全無人化的持續學習。
> 當我在左邊的 Label Studio 按下第 100 張修正照片的 Submit 時... (按下按鈕)
> 你們會看到右邊的 ClearML 系統『全自動』彈出了一個新的重訓任務！
> 從發現誤判、資料微調，到啟動模型重訓，完全不需要工程師寫任何一行 Code 或手動搬運檔案，AI 會每天自己變聰明！」

---

## 🔄 第 6 步：手動觸發模型進化 (MLOps Retraining)

雖然系統會累積滿一定數量的誤判後自動觸發重訓，但如果您想要**「立刻」**讓模型吸收您剛剛在 Label Studio 上的標註，您可以隨時手動啟動這套進化流程。我們已經將所有步驟打包成一鍵執行檔！

- **在哪裡執行**：任何一個閒置的終端機
- **執行指令**：
  ```bash
  cd /Users/albert/Documents/專案/AIPE03/Fall
  ./run_mlops_retrain.sh
  ```
**這個一鍵腳本會自動完成以下驚人操作：**
1. 📥 **自動下載標籤**：連線至 Label Studio 抓取您最新的按鈕標註 (Action_Recognition_Video_V3 專案)。
2. 🦴 **自動特徵提取**：呼叫 YOLO-Pose 把這批影片的骨架特徵全部壓榨出來。
3. 🥊 **擂台賽對決 (Champion vs Challenger)**：啟動 Transformer 訓練。訓練完畢後，它會自動將新模型的準確率與歷史最佳模型進行「生死決鬥」。
4. 🚀 **零停機熱更新**：如果新模型挑戰成功，它會自動將其轉為 ONNX 格式，並直接熱更新 (Hot-Swap) 到模型庫，前線攝影機瞬間升級！
